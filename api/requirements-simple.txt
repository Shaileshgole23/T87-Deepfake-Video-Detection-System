# Simplified requirements without dlib/face_recognition
# This allows the app to run in mock mode

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Deep Learning (CPU version)
torch==2.2.0
torchvision==0.17.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Computer Vision (basic)
opencv-python==4.9.0.80
Pillow==10.2.0

# Data Processing
numpy==1.26.3

# Note: dlib and face-recognition are optional
# The app will work in mock mode without them
